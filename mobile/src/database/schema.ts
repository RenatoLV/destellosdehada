/**
 * SQLite schema is defined and evolved exclusively by migrations.
 * Keep this exported version marker for diagnostics; do not add duplicate DDL
 * here because it can diverge from the installed database.
 */
export const SQLITE_SCHEMA_VERSION = 13 as const;
